<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PeerView - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        .home-hero {
            background: url('https://images.unsplash.com/photo-1506784926683-7f3f8e0e7a7e?q=80&w=2070&auto=format&fit=crop') no-repeat center center;
            background-size: cover;
            padding: 4rem 0;
            color: white;
            position: relative;
            border-radius: 15px;
            overflow: hidden;
        }
        .home-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(189, 187, 236, 0.7), rgba(124, 58, 237, 0.7));
            z-index: 0;
        }
        .home-hero .content {
            position: relative;
            z-index: 1;
            animation: fadeIn 1.5s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="bg-light">
    <%- include('partials/navbar', { user: user }) %>
    <div class="container mt-5">
        <section class="home-hero">
            <div class="content text-center">
                <h1 class="display-3 fw-bold">Welcome to PeerView</h1>
                <p class="lead">Collaborate, upload, and review projects with AI-powered insights.</p>
                <% if (user) { %>
                    <p class="mt-4">Hi, <%= user %>!</p>
                    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                        <a href="/create-project" class="btn btn-primary btn-lg me-md-2">Upload Project</a>
                        <a href="/projects" class="btn btn-secondary btn-lg me-md-2">View Projects</a>
                        <a href="/logout" class="btn btn-outline-danger btn-lg">Logout</a>
                    </div>
                <% } else { %>
                    <div class="mt-4 d-grid gap-2 d-md-flex justify-content-md-center">
                        <a href="/signup" class="btn btn-primary btn-lg me-md-2">Get Started</a>
                        <a href="/login" class="btn btn-secondary btn-lg">Login</a>
                    </div>
                <% } %>
            </div>
        </section>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>